{% extends "::base.html.twig" %}

{% block title %}AppBundle:Post:theme {% endblock %}

{% block body %}

    <div class="container">
        <table class="table  table-striped " style=" width: 500px; margin-right: 250px; margin-top: 100px">
            <caption> <h2>{{ title }}</h2> </caption>
            <thead >

            </thead>
            <tbody >
            {% for theme in themes %}
                <tr>

                    <td> <a href="{{ path('post', {'id':theme.id }) }}"> {{ theme.nomTheme }}</a></td>
                    <td>{{ theme.nbRep }}</td>
                </tr>

            {% endfor %}

            </tbody>

        </table>
    </div>
{% endblock %}
