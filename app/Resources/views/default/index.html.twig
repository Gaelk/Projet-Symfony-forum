{% extends "::base.html.twig" %}

{% block title %}AppBundle:Post:theme {% endblock %}

{% block body %}

    <div class="container">
        <table class="table  table-striped " style=" width: 500px; margin-right: 250px; margin-top: 100px">
            <caption> <h2>{{ title }}</h2> </caption>
            <thead >

            </thead>
            <tbody >
            {% for theme in themes %}

                <tr>
                    <td><a href="{{ path('theme', {'id':theme.id}) }}"> {{ theme.nomTheme }}</a></td>
                    <td>{{ theme.nbPost }}</td>

                    {#{% for colName in theme | keys %}#}

                        {#<td>#}
                            {#{% if colName=="theme" %}#}
                                {#<a href="{{ path('theme', {id:1} )}}">  {{ theme[colName] }} </a>#}
                            {#{% else %}#}
                                {#{{ theme[colName] }}#}
                            {#{% endif %}#}
                        {#</td>#}
                    {#{% endfor %}#}
                </tr>

            {% endfor %}

            </tbody>

        </table>
    </div>
{% endblock %}
